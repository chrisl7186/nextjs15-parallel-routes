import React from 'react';
import {CardContent} from "@/components/ui/card";

export default function Loading({}) {
    return (
        <CardContent>
            <div>loading...</div>
        </CardContent>
    );
}
